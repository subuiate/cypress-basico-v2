name: End-to-end tests 🧪
on: [push]
jobs:
  cypress-run:
    runs-on: windows-2019
    steps:
      - name: Cypress run
        uses: cypress-io/github-action@v5
        with:
          build: npm run build
          start: npm start
          browser: chrome